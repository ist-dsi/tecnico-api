allOf:
  - $ref: "./WrittenEvaluation.yaml"
  - type: object
    required:
      - isEnrolled
    properties:
      isEnrolled:
        type: boolean
        description: Whether the student is enrolled in this evaluation or not.
