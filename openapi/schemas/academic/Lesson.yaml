type: object
required:
  - start
  - end
properties:
  start:
    type: string
    format: date-time
  end:
    type: string
    format: date-time
  room:
    allOf:
      - $ref: "./Space.yaml#/BasicRoom"
